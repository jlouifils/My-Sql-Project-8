extends layout

block content
  h1= Library
  h3= please do not hesitate to donate to our collection of books.
  p#header
    a.button(href='/books/new') add a book
  table
    thread
    tr
      th Title
      th Author
      th Genre
      th Year
    tbody
      each book in books
            tr
               td
                  a(href='/books/' + book.id)= book.title
               td= book.author
               td= book.genre
               td= book.year
